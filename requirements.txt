# Kaggle already ships torch and numpy. There you only need:
#   pip install -q tiktoken gradio
torch>=2.2
numpy>=1.24
tiktoken>=0.6
gradio>=5.0
matplotlib>=3.7

# local pre-tokenization only
datasets>=2.19
pyarrow>=15.0
tqdm>=4.66

# optional, only for kaggle_driver.py
kaggle>=1.6
